# Malloc---Custom-Dynamic-Memory-Allocator
A robust, POSIX-compliant implementation of the C dynamic memory library (malloc, free, realloc, calloc) using mmap and featuring block splitting, immediate fusion, and strict data alignment.
